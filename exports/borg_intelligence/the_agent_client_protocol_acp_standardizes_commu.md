# The Agent Client Protocol (ACP) standardizes communication between code editors/IDEs and coding agents to solve integration overhead and compatibility limits.

## 📋 Metadata
- **Category**: Connectivity & Interoperability (MCP/A2A)
- **Innovation Score**: 8/10
- **Researched At**: 2026-03-20 02:06:32
- **Source**: [https://agentclientprotocol.com/overview/introduction](https://agentclientprotocol.com/overview/introduction)

## 🧠 Technical Summary
The Agent Client Protocol (ACP) aims to standardize the interface between code editors/IDEs and AI coding agents, analogous to how the Language Server Protocol (LSP) standardized language server integration. This standardization addresses the current problem where every editor needs custom integrations for every agent, leading to significant integration overhead and limited compatibility. ACP supports both local agent execution (using JSON-RPC over stdio) and remote scenarios (via HTTP or WebSocket). The protocol reuses JSON representations from the existing Messaging Communication Protocol (MCP) where appropriate but introduces custom types necessary for agentic coding user experiences, such as displaying code diffs. User-readable text defaults to Markdown for flexible rich formatting.

## 🛠️ Key Features
Standardized communication protocol, Support for local (stdio/JSON-RPC) and remote (HTTP/WebSocket) agents, Custom types for agentic UX elements (e.g., diffs), Markdown as default text format, Decoupling of agent and editor development.

## 🏷️ Tags
#acp, #protocol, #ide-agent communication, #standardization, #json-rpc, #websocket, #lsp analogy, #interoperability, #agent integration, #remote agents

---
*Generated by Bobby's Research Command v0.1.0*
